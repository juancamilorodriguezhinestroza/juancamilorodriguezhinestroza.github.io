var nota

nota=parseInt(prompt("ingrese la nota"));

if (nota<=3 ||nota>=3 ) {
    document.write("el esctudiante aprovo con " +nota+"")
} else {
    document.write("el esctudiante reprovo con  " +nota+"")
}